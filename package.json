{
  "name": "bhoomi-setu",
  "version": "1.0.0",
  "description": "Blockchain-powered land registry system",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run blockchain\" \"npm run backend\" \"npm run frontend\"",
    "dev:full": "concurrently --kill-others-on-fail \"npm run blockchain\" \"npm run deploy\" \"npm run backend\" \"npm run frontend\"",
    "blockchain": "cd contracts && npm run dev",
    "deploy": "timeout /t 10 /nobreak >nul && cd contracts && npm run deploy:local",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../contracts && npm install",
    "build": "cd contracts && npm run build && cd ../backend && npm run build && cd ../frontend && npm run build",
    "start": "concurrently \"npm run backend:start\" \"npm run frontend:start\"",
    "backend:start": "cd backend && npm start",
    "frontend:start": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "blockchain",
    "land-registry",
    "ethereum",
    "nextjs",
    "nodejs"
  ],
  "author": "Bhoomi Setu Team",
  "license": "MIT"
}
